pipeline {
   agent any
   stages {
       stage ('Compile') {
           steps {
                 sh './gradlew compile'
               }
           }
       
        stage ('Build') {
           steps {
               
                   sh './gradlew clean build'
               }
           }
		}
   }
