pipeline {
   agent any
   stages {
       stage ('Compile Stage') {
           steps {
                 sh './gradlew compile'
               }
           }
       }
        stage ('Build Stage') {
           steps {
               
                   sh './gradlew clean build'
               }
           }
   }
